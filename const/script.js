window.onload = function () {

    const pi = 3.142;
    
    function calcArea(r) {
        // se você criar uma const aqui (local const), ela pode ter um valor diferente
        // desse jeito truva
        console.log ("The area is: " + pi * r * r);
    }

    calcArea(5)

   // console.log(pi);

}